# Industrial-AIOps (iaiops) — 高层设计 (HLD)

> 单一架构真相文档。此前 `CLAUDE.md` 指向的 `docs/HLD.md` / `docs/PLATFORM-ARCHITECTURE.md`
> 均不存在；本文件补上并成为权威来源。身份、品牌隔离、铁律见 `CLAUDE.md`。

## 1. 定位

厂商中立的**现场勘察 + 工业数据 tap + 跨协议智能排查**。唯一带**审计 + 预算 + 回滚 +
分级审批**的 OT 运维工具线。核心承诺一句话：

> **数据采集与操作要准确、高效；每一次调用都留下不可绕的审计。**
> 读/写**授权**不由 tap 承担 —— 那是 agent 判断或账号/权限管理的职责。

**三件事，顺序不能颠倒**（0.23 起「勘察」是一等能力，见 §9）：

| | 回答什么 | 入口 |
|---|---|---|
| **勘察 discovery** | 这个网段上**有什么** | `iaiops scan` |
| **数据 tap** | 从已知端点**取什么** | 各协议 `read` |
| **排查 brain** | 取到的数据**说明什么** | RCA / OEE / 基线 … |

第一件是 0.23 才补上的。在此之前所有能力都默认「你已经知道端点」——
而 brownfield 现场恰恰不知道，这是上手断层的根源（§9）。

**LLM 是可选的，且这是结构性事实而非当前状态。**
`core/brain` · `core/discovery` · `core/runtime` · `connectors` 四层**不得**出现任何
通向模型的 import，由 `tests/test_brain_is_llm_free.py` 守卫（AST 扫描，136 个文件）。
模型只出现在两处、都不承重：`rca_narrate` 把**已算完、已带引用**的裁决换成人话；
agent 前端决定**调哪个工具**。拿掉模型，输出逐字节相同。

推论：**agent 前端与纯 App 前端不是两套逻辑，是同一引擎的两个前端** ——
差别只在「谁决定调哪个工具」。边缘离线场景用纯 App，能力零损失。

## 2. 四层架构

```
┌───────────────────────────────────────────────────────────────┐
│ Front-ends（前端边界，各自受治理）                              │
│   • mcp_server/  —— 菜单式 MCP：@mcp.tool() 包装，按 IAIOPS_MCP  │
│                     profile 暴露；两道注册期 posture 门          │
│   • iaiops/cli/  —— Typer CLI：每条命令一个受治理边界            │
├───────────────────────────────────────────────────────────────┤
│ core/governance/ —— 治理主干（两个前端共用同一引擎）            │
│   @governed_tool = 策略 + 审计 + 预算 + 风险分级 + 审批 + 回滚   │
├───────────────────────────────────────────────────────────────┤
│ core/brain · core/runtime —— 跨协议分析（只读、纯函数、advisory）│
│   + 归一化数据模型 + 连接/配置/信封                             │
├───────────────────────────────────────────────────────────────┤
│ core/discovery/ —— 现场勘察（L0 被动 → L1 温和扫描 → L2 识别）  │
│   端口白名单 + 限速 + wire log + 无写路径守卫；**不经 connectors**│
│   它的产物是「端点长什么样」，正是 runtime/config 的输入         │
├───────────────────────────────────────────────────────────────┤
│ connectors/<protocol>/ops.py —— 纯协议层                        │
│   接收已解析的 target 对象 + 操作参数；不含治理、不含 endpoint 名 │
└───────────────────────────────────────────────────────────────┘
editions = skills/<edition>/  按行业打包 + 各自 signature 工具
```

**层间调用**：`mcp 包装` 与 `cli 命令` 都持有 `endpoint` 名 → 解析成 `target` →
调 `connectors/*/ops.py`。**endpoint 名只存在于前端边界**，进入 ops 后只剩已解析对象。

## 3. 治理主干 `@governed_tool`

`iaiops/core/governance/decorators.py`。挂在**前端边界函数**上，一次调用跑一次：

| 职责 | 说明 |
|---|---|
| 策略预检 | deny 规则、维护窗口 |
| 审计 | 每次调用写 `~/.iaiops/audit.db`（参数脱敏 + 控制字符清洗 + 密钥擦除） |
| 审计健康门 | high/critical 在审计不可写时**拒绝**（fail closed）；low/medium 只读放行并告警一次 |
| 预算 / 失控保护 | 上限按「干的活」计（次数与时长，拒绝不计）；失控窗口按 `(tool, params)` 计，**拒绝也计** |
| 分级审批 | high/critical 需要**已记录的审批人**（一次性 token 优先，静态 env 兜底） |
| 回滚 | 成功的写捕获逆操作到 `~/.iaiops/undo.db`，结果带 `_undo_id` |

**审计状态必须反映真实结果（0.20.3）。** 工具不抛异常——MCP 层把每个异常转成规范的
`{error, hint}` 信封，于是治理包装器看到的是一个正常返回值，曾把失败的调用记成
`status='ok'`。两个后果：审计轨迹分不清「写成功」和「PLC 连不上」；`_finalize` 用
`success=(status=='ok')` 汇报给 pattern 熔断器，**一个每次都失败的 armed pattern 被汇报成每次都成功**。
现在识别该信封并记 `status='error'`，同时跳过失败调用的 undo 计算。识别范围刻意收窄：
只认信封的两种结构形态且 `error` 为非空字符串——诊断工具返回 `error: None`、结构化
`{code, text}`、或含一行错误的多行结果都不算失败。

**失控窗口必须看得见被重试的拒绝（0.20.3）。** 上限与失控窗口是两条轴，一次策略拒绝把它们分开：
被拒绝的调用免于上限是对的（它没干活，一条配错的 deny 规则不该耗光操作员预算），但失控指纹
过去只记在放行路径上，于是熔断器能看见所有循环、唯独看不见**最可能的那一种**——一个不理解
拒绝、于是无限重试的调用方（LLM agent 最容易产生的失控形态）。实测：500 次相同的被拒高危写、
上限 10、零次拦停。现在拒绝计入窗口且**只**计入窗口；窗口填满后熔断器的 `BudgetExceeded`
取代那次拒绝（此时调用方需要的是「别再叫了」而不是第 26 条「被拒绝」），并审计成
`budget_exceeded`，使操作员能区分「被拒绝」与「被拒绝后不肯停」。

**scoping 依赖 endpoint 名**：策略、审批分级都按 endpoint 匹配。这是治理必须留在
**前端边界**（endpoint 名仍在）、而**不能**下沉到 ops 层（只剩已解析 target）的根本原因。

### 3.1 双前端、单引擎（本轮的核心不变量）

MCP 与 CLI 是两个独立前端，**没有任何一次调用同时穿过两者**。因此：

- **每个前端在自己的边界各自受治理**（各挂一次 `@governed_tool`），
- **共用同一个治理引擎**（同一个 audit.db / policy / budget）。

于是 `iaiops ethercat write-sdo --apply`（CLI 写）与 `ethercat_write_sdo`（MCP 写）
产生**形状一致**的审计行。任一前端都无法绕过审计。

写操作用**按实际效果定级**（effect-based risk），**两个前端一致**：dry-run 预览
按 `low` 审计——它什么都不改，故不需审批人，预览一次写不该先去领 token；真正的写按
`high`——审计 + 审批门禁（MOC）。两者都留审计行。机制在共享的 `@governed_tool`
（`preview_param`）里：MCP 写工具用 `preview_param="dry_run"`（`dry_run` 为真=预览），
CLI 写命令用 `preview_param="apply", preview_truthy=False`（`apply` 为假=预览）。
该参数**默认关闭**，故 iaiops-energy / iaiops-enterprise 等复用本装饰器的仓库行为不变，
除非它们也接入。

> 曾经的缺口（已在本轮修复）：`@governed_tool` 只挂在 MCP 包装上，CLI 直接调 `ops.*`，
> 导致 **CLI 写操作零审计**。修复 = 给 CLI 命令边界同样挂治理。

## 4. MCP 注册期 posture 门（与读写授权正交）

两道门在注册后、`assert_all_tools_governed()` 前运行，**把工具从注册表删除**（而非调用时拒绝），
因为弱模型/被注入的模型能调用它**看得见**的任何工具，而一次 OT 写不可逆：

| 门 | env | 依据 | 语义 |
|---|---|---|---|
| **no-egress** | `IAIOPS_NO_EGRESS=1` | `_egress` | 删掉"把数据发出盒子"的工具（airgap/密闭部署） |

> **read-only 门（`IAIOPS_READ_ONLY`）已于本轮移除**。它把一个**读写授权决定**焊进了 tap，
> 与定位冲突（授权归 agent/账号管理）。移除后，**审计成为唯一扛得住的保证**，故同步补齐
> CLI 审计（§3.1）。no-egress 门保留 —— 它回答的是另一条轴"数据能否离开盒子"，属部署拓扑
> 保证，非读写授权。

## 5. 返回信封（弱模型友好，独立机制）

`iaiops/core/runtime/envelope.py`：给 list/bounded 结果统一附加截断元数据。与授权无关，保留。

## 6. 决策记录

| # | 决策 | 理由 |
|---|---|---|
| D1 | 移除 read-only 门 | 读写授权非 tap 职责；焊进采集层是错位 |
| D2 | 保留 no-egress 门 | 正交轴（数据外泄/airgap），部署拓扑保证 |
| D3 | 治理留在前端边界，不下沉 ops | ops 层已丢失 endpoint 名，策略/审批 scoping 会失效 |
| D4 | CLI 命令边界补挂 `@governed_tool` | 双前端单引擎，审计不可绕，与 MCP 形状一致 |
| D5 | 保留 brain/connectors 中"read-only/非破坏性"**描述性**注释 | 描述行为，非被删功能 |
| D6 | **拒绝** MCP Tunnels（私网反向穿透） | 隔离是产品承诺本身，不是待解决的障碍 —— 见 §7.2 |
| D7 | **拒绝** 无状态传输 / 云托管横向扩；本地 stdio 是一等公民 | 协议会话天生有状态且昂贵；部署形态是现场边缘盒子，无横向扩需求 —— 见 §7.3 |
| D8 | Tasks 扩展 **defer**，设计先行 | 异步拆开了「调用—审计—结果」同步闭环，治理语义未定 —— 见 §7.4 |
| D9 | MCP Apps **有条件评估**，仅限「人在回路」场景 | 需 host 能渲 iframe；无人值守的边缘 agent runtime 用不上 —— 见 §7.5 |
| D10 | 治理不变量必须由**覆盖真实工具面**的契约测试守住，而非注释 | 合成函数上的测试全绿，真实写工具却可能在重构中丢掉 `risk_level="high"`；已实测：把 `s7_write_db` 降级会挂 3 条契约测试，而在补契约之前一条都不挂 |
| D11 | 被拒绝的调用免于**上限**、但计入**失控窗口** | 两条轴：上限给「干的活」计价（拒绝没干活，配错的规则不该耗光预算），失控窗口探测卡死循环（重试拒绝正是最常见的那种） |
| D12 | 「某依赖不可 CI 构建」是**有保质期的测量**，不是属性 | `pydnp3` 被当作不可构建数月（实为 2019 绑定层三处机械问题），`pyiec61850` 早已能跑却无人回头核对 —— 重复该结论前必须重测；且 live 测试被 skip 必须让构建变红 |
| D13 | open-core 分界 = **单次调用的能力**留 MIT，**组织与时间维度的保证**归 `iaiops-enterprise` | 见 §8.3。分界依据是"问题属于谁"，不是"哪块值钱" —— 后者会把边界承诺逐次侵蚀成空话 |
| D14 | **拒绝**把已发布仓库转私有、**拒绝** relicense（BUSL/AGPL/SSPL） | MIT 不可撤销：转私有挡不住已发布的 0.22.0，只损失五个分发渠道。稀缺的是分发与责任承担，不是 connector 代码（协议层是开源库之上的薄壳，两周可重写） —— 见 §8.4 |
| D15 | 新功能**默认落 MIT**；进 enterprise 需 §8.3 判定为真且写进 `iaiops-enterprise/docs/ROADMAP.md` | 默认方向决定十年后的形状。默认收费 = 每次都要论证"为什么不收费"，边界必然漂移 |
| D16 | **就绪度是一等概念**：`readiness` 只报告、**绝不代填**语义映射 | 猜错产量计数器产出的是**看起来正确的** OEE 数字，比报错危险得多 —— 见 §9.4 |
| D17 | `onboard`/`readiness` **CLI 与 App 页面都做**，共用一个引擎 | 与 D-LLM 同理：引擎出结构化结果，前端只负责渲染。点表确认天然是表格交互，CLI 用「导出 CSV→人改→导入」兜底，不在命令行里逐行问 |
| D18 | **拓扑/关系图是分析输入，不是可视化需求** | RCA 目前只按时间加权，上游停机会持续产生一串同置信度的下游假根因；关系是第二根轴 —— 见 §10.3② |

## 7. MCP 协议版本立场（2026-07-28 spec）

### 7.1 没有 flag day —— 我们处在 opt-in 位置

MCP 有三个**各自独立、版本协商**的东西，不是一个整体：协议 spec（带日期的版本）、server 声明支持哪个版本、客户端/host 连接时协商。握手时双方谈妥一个都支持的版本 → 新旧共存。**"spec 升级了" ≠ "server 必须跟"。**

升级链条与我们的位置：

```
① spec 发布(2026-07-28, RC)  ②SDK 实现  ③host 支持  ④我们 opt-in ★  ⑤版本协商让新旧并存
```

在没有主动升 SDK、没启用新特性之前，①②③ 发生什么都不影响现有部署。**触发行动的只有我们自己的选择，不是外部强制。**

事实核对（2026-07-30 实测，勿凭转述）：Python SDK **`mcp 2.0.0` 已是正式版**（PyPI 2026-07-28 发布，`Development Status :: 5 - Production/Stable`，未 yanked），不是 beta —— "等 SDK GA 再升"这个条件**已经满足**。真正让我们不急的是别的：pin `mcp[cli]>=1.10,<2.0` 已挡住被动升级，且本次 stateless 类改动主要针对 Streamable HTTP，我们走 stdio 几乎不受影响。

⚠️ SDK v2 是唯一早晚要碰的**机械迁移**：`mcp.server.fastmcp` 模块已删除，`_GovernedFastMCP(FastMCP)` 在本仓与 `iaiops-energy` 都会 ImportError。但 `MCPServer.tool` 签名与 `FastMCP.tool` **逐字相同**，子类换一行基类即可；`ToolAnnotations` 字段改 snake_case 但 camelCase kwargs 仍被 pydantic alias 接受、wire 仍是 camelCase，故 `mcp_server/hints.py` 构造代码不变 —— **只有测试里的 `.readOnlyHint` 属性访问会断**。

### 7.2 D6 — 拒绝 MCP Tunnels

这条与 IT 线（`AIops-tools`）结论**相反**，必须显式记录，避免被跨线经验带偏。IT 线把私网反向穿透视为"够得着管理网"的关键钥匙；对 OT 线，网络隔离**是产品承诺本身**：

- `IAIOPS_NO_EGRESS` 在注册期就把外发类工具从 `list_tools()` 摘掉（§4）
- `deploy/airgap/compose.yaml` 是一等公民部署形态
- Margo 描述符写明 outbound-only、零入站端口
- 等保 2.0 / IEC 62443 分区分域交叉表

一条从 OT 网反向连出公网的隧道，正是上述整套要排除的东西。（Tunnels 本就是某托管平台的产品功能，不属于 MCP 协议。）

### 7.3 D7 — 拒绝无状态托管，本地 stdio 保持一等公民

两个独立理由：

1. **协议会话天生有状态且昂贵**：OPC-UA session、S7 连接、MQTT/Sparkplug 订阅、BACnet COV 订阅 —— `ConnectionManager` 存在的全部意义就是复用它们。无状态 = 每次调用重建会话，对老 PLC 是实打实的负担（部分设备连接数上限个位数）。
2. **部署形态本就不是云托管**：IGEL recipe / Margo edge app / airgap compose 全是"跑在现场那台盒子上"。一个厂一个 tap，不存在"一个端点服务整个团队"的横向扩需求。

**底线**：企业向复杂度（OAuth/OIDC、连接池、无状态）若将来引入，必须是**可选增量**，不得渗入本地/边缘路径 —— 否则破坏「弱模型/边缘模型兼容」这条家族级约束（§5）。OAuth 与 D1 不冲突（边界**认证**，非 tap 内**授权**），但 OT 现场常连不到 IdP（airgap 就是没有），故不得成为必需。

### 7.4 D8 — Tasks defer，四个治理问题先答

候选（真正耗时的操作，比 IT 线少得多 —— 绝大多数工具是读且快）：`monitor_changes`（bounded COV）、HART burst-listen、MTConnect stream、跨端点发现（PROFINET DCP / BACnet Who-Is / `asset_inventory`）、baseline 学习。**写操作不是候选** —— dry-run + MOC 审批的延迟是人的延迟。

动手前必须回答：

1. task 的审计行**何时**写？现有 `@governed_tool` 假设「调用—审计—结果」是同步闭环，Tasks 把它拆开了。
2. **取消一个已把写请求发到 PLC 的 task，语义是什么？** 协议层已发出的帧收不回来，undo 捕获的 before 状态还算不算数？
3. budget 在 task 生命周期哪个点扣？
4. effect-based 风险降级（`_effective_risk_level`，preview 走 low）在异步下如何表达？

审计的不可绕过性是本产品线的核心承诺（D1/D4）。**设计未定不写实现。**

### 7.5 D9 — MCP Apps 有条件评估

本次 spec 里**唯一**直接服务「边缘小模型」约束的能力：富内容渲染进沙箱 iframe，模型只拿紧凑摘要，大输出不再逐 token 灌 context。候选是报表类只读输出：RCA 叙述、OEE 报表、ISA-18.2 alarm Pareto、`asset_inventory` 全表、等保/62443 合规报告、historian 序列。

**前提陷阱**：需 host 能渲 iframe。「工程师用桌面客户端连现场 tap」有效；「边缘盒子上无人值守的 agent runtime」没有 UI，完全白搭。评估第一步是确认目标场景占比，别为用不上的形态做工程。

**不要指望它解决工具数问题** —— 本次 spec 对「工具数 / 选对工具的准确率」一字未提，那条杠杆仍是 `EDITION_MODULES` 菜单式 profile。

## 8. 商业分层与开源边界（open-core）

架构分层（§2）回答"代码放哪"，本节回答**"功能归哪个仓、按什么授权"**。这条线一旦
含糊，它会被每一次"这个好像能卖钱"逐次侵蚀，最终把 §8.2 的承诺变成空话 —— 所以
判定规则写在架构文档里，和治理不变量同级。

> 本节只写**边界与归属**（公开、对用户的承诺）。定价、市场、销售路径属于商业策略，
> 不进本仓 —— 见私有仓 `iaiops-enterprise/docs/COMMERCIAL.md`。

### 8.1 三层

| 层 | 载体 / 授权 | 内容 | 形态 |
|---|---|---|---|
| **Community** | 本仓 + `industrial-aiops-energy`，**MIT** | tap 能力全集：12+ 协议的读**与写**、治理主干（审计/预算/回滚/审批 token/posture 门）、brain 跨协议分析、全部 edition skills | 五渠道公开分发（PyPI · GitHub · MCP registry · SkillHub · Glama） |
| **Enterprise** | `iaiops-enterprise`，**proprietary** | 围绕写的**组织工作流与可交付凭证**：n-of-m 审批、签名审计证据、合规证据报告、留存归档、fleet、license | 私有 wheel / 私有 index，随支持合同交付 |
| **Project** | 非软件 | 现场接入、点表梳理、验收报告、版本 pin 与兼容承诺 | 人天 |

**Enterprise 不是"功能更多的 iaiops"**，它是支持合同的技术载体：买方买的是**责任与可交付
的证明**，软件只是它的形状。这也是为什么 §8.3 的判定标准是"问题属于谁"而非"哪块值钱"。

### 8.2 不可退让的边界承诺（已在 `iaiops-enterprise/README.md` 公开）

1. **已开源的永不收回** —— MIT 基座的全部功能（**包括受治理的写工具**）永久开源可用；
   enterprise 层不 gate、不降级、不弱化基座。
2. **离线优先，永不 phone-home** —— license 是离线可验的 Ed25519 文件；企业功能在气隙内完整可用。
3. **过期不 brick** —— 30 天宽限，之后仅企业功能给 teaching error；开源基座不受任何影响。

第 1 条在 OT 语境下不是姿态而是准入条件：现场买方最怕关键路径被厂商卡脖子，反悔一次，
这条产品线的可信度归零。**写路径 100% 复用开源治理链**（dry-run → 一次性 token → undo →
审计），enterprise 只决定"token 由谁、按什么流程铸出"。

### 8.3 新功能归属判定（唯一规则）

按顺序问，命中即停：

1. 它让 tap **读到/写到**一个新的设备、协议、数据点或分析结论吗？→ **MIT**。永远。
2. 它让**某一次调用**变得可审计、可回滚、可预算、可分级吗？→ **MIT**。治理主干是基座
   的定位本身（§1），不是增值项。
3. 它解决的是**多个人 / 多个站点 / 多个月**的问题吗？—— 谁有资格批、谁签名背书、
   跨站怎么统一、留存多久、最后交给**外部第三方**（审计方/测评机构/监管）看什么？
   → **enterprise**。
4. 以上都不是 → **MIT**（默认方向，见 D15）。

一句话：**单次调用的正确性归 MIT；组织与时间维度的保证归 enterprise。**

明确**不得**迁入 enterprise 的东西（迁入即违反承诺 1）：任何协议 connector、任何
`risk_tier` 分级、审计写入本身、undo 捕获、dry-run、posture 门、返回信封、edition skills。

边界上的两个真实判例：

- **合规交叉表 vs 合规证据报告** —— `core/brain/compliance*.py` 描述的是"**产品**如何对上
  等保 2.0 / IEC 62443"，与本站运行数据无关，是自评/onboarding 材料 → **留 MIT**。
  而"**本站** 在过去 90 天里发生了 N 次写、每次都有 2 名具名审批人、哈希链完整、由站点
  密钥签名"是一份可交给第三方的**背书**，属规则 3 → **enterprise**。同一份交叉表，
  两种产物，不冲突。
- **审计转发 vs 可靠投递** —— `iaiops audit forward`（syslog UDP / HTTP，best-effort、
  at-least-once、无 ACK）是单次调用之后的顺手动作 → **留 MIT**。"六个月不丢一条"是
  留存承诺，属规则 3 → enterprise。
- **现场勘察（scan）的两个前端** —— 扫描引擎、`iaiops scan` CLI、本地 SQLite 存储、
  未签名 HTML 报告全部 **留 MIT**（规则 1：它让 tap 读到一台此前不知道的设备）。
  跨时间的快照比对、跨站点汇总、以及**给第三方看的签名报告**属规则 3 → enterprise。
  **scan 的 MCP 前端也在 enterprise**，这是本条唯一需要解释的地方：它看起来像"把一个
  MIT 能力的入口收进付费层"，但并不违反承诺 1 —— 能力（库 + CLI）完整留在 MIT 且可
  独立使用，MCP 只是第二个前端；而 scan 按定位就是**零 AI、离线**的产品（边缘侧不跑
  大模型），agent 前端服务的是办公室侧而非现场侧。若哪天现场侧真的需要 agent 驱动
  扫描，按 D15 默认方向应当落回 MIT。

### 8.4 D14 —— 为什么不转私有、不 relicense

三条独立理由，任一成立即足够：

1. **收不回来。** MIT 授权不可撤销：0.22.0 及此前所有版本，任何人可永久 fork、商用、
   闭源分发。转私有只影响未来提交。
2. **代价与收益反向。** 稀缺的是"被五个渠道搜到 + 被 LLM 装上 + 有版本 pin 和支持矩阵
   的承担者"，不是 connector 代码 —— 协议层是 `asyncua`/`pymodbus`/`snap7` 之上的薄壳，
   一个懂 OT 的人两周可重写。转私有 = 丢掉唯一稀缺的，换回不稀缺的。
3. **BUSL/AGPL 解决的是"被白嫖"，当前问题是"没人用"。** 加限制会同时压住 MCP 生态与
   Margo/IGEL 那条路。等出现真实的商业抄袭再重议 —— 届时受影响的也只是**新增**代码。

---

## 9. 能力就绪阶梯（onboarding 的架构真相）

> 2026-08-19 新增。起因：一份介绍 deck 把 9–20 页全用来讲「能得到什么」，却没有一页讲
> 「在此之前你必须先有什么」。陌生用户装完之后**不知道下一步做什么**，而这不是文档
> 问题 —— 是架构里一直没有把「就绪度」当成一等概念。

### 9.1 三层自给度

能力不是平的。按「需要人提供什么」分成三层，这决定了 onboarding 能自动到哪一步：

| 层 | 需要人给什么 | 典型能力 |
|---|---|---|
| **自给** | 一个网段，或什么都不要 | `scan`、数据质量评分、采样、CSV/Parquet 导出、Prometheus 端点 |
| **半自给** | 位号名字规范就能猜中 | 语义分类（`classify_tag` 启发式；**猜不中返回 `other`，绝不硬猜**）、Modbus 厂商模板 |
| **须人给** | 工艺知识，工具推不出来 | 关键位号清单、OEE 的运行/停机/产量/节拍映射、工艺上下游、根因权重初值 |

**这三层之间没有自动升级路径，也不该假装有。** 「哪个位号重要」是工艺知识，
不是可推断的信号属性；一个猜它的工具会在最要命的地方悄悄猜错。

### 9.2 旗舰能力的真实前置

`downtime_rca` 是**纯函数** —— `alarms` / `tags` / `dataflow` / `historian` 全是**传入
参数**，它自己不采集。所以「一次调用出根因」成立的前提是现场**已经**具备：

| 前置 | 谁提供 | 缺了会怎样 |
|---|---|---|
| 端点配置 | `scan` 可生成草稿 | 无法连接 |
| **关键位号清单**（`refs`） | **人** | 采不到 tag 证据 |
| 告警流 | **仅 OPC-UA**（A&C 事件）；其它协议返回空 | 少一整类证据 |
| 历史库（`historian:` 块） | 人配 | **看不到事发前的恶化** —— `*_live` 采的是「现在」 |

**四条里三条要人**。任何演示材料若不标注这一点，现场第一次实跑就会翻车。
证据不足时输出会降级为 `insufficient_evidence` 并列出「还需采什么」——
这是设计意图，不是故障。

### 9.3 就绪度必须可查询

推论：需要一个**随时可跑**的自检，回答「我现在能跑哪些场景、还差什么」，形如：

```
✅ 数据质量看板     可跑
✅ 导出 / Grafana   可跑
⏳ 保守基线预警     需 7 天样本（已有 0 天）
❌ 停机根因（完整） 缺：历史库连接、告警源（当前协议无 A&C）
❌ OEE              缺：运行/停机/产量/节拍 四类位号映射
```

它不只是 onboarding —— 现场每次变更后都该重跑，也是给客户看的成熟度基线。
~~**尚未实现**~~ → **已发布 #166**（2026-08-24 更正）：`iaiops readiness`，
引擎在 `iaiops/core/readiness/`。零联网,并且实现了本节 §9.4 的边界 ——「只报告,永不替你填」。

### 9.4 边界

就绪度自检**只报告，不代填**。它不猜哪个位号是产量计数器，不替人做工艺映射 ——
那会把 §9.1 的「须人给」偷偷降级成「猜」，而错误的映射产出的是**看起来正确的**
OEE 数字，比报错危险得多。

---

## 10. 分析能力的两条已知短板与 onboarding 形态（2026-08-19 定）

### 10.1 前端形态：CLI 与页面**都要**

`onboard` 与 `readiness` 各出**两个前端、共用一个引擎**（与 §1 的 LLM 可选性同一条理由）：

| 前端 | 场景 |
|---|---|
| CLI 向导 | 工程师 SSH 进边缘盒、CI、无 GUI 的工控机 |
| App 页面 | 现场第一屏；点表确认这类**必须人看**的步骤，表格远胜命令行问答 |

**不是两套逻辑**：引擎产出结构化结果，CLI 渲染成文本、页面渲染成表格。
点表语义确认那一步天然是表格交互（几十上百行要勾选/改类别），CLI 只做到"导出 CSV
让人改完再导入"即可，不必在命令行里做交互式逐行确认。

### 10.2 `iaiops readiness` = 成熟度命令

不只是 onboarding 的一步，而是**随时可跑的成熟度基线**：现场每次变更后重跑，也用于
对客户说明"你要投入什么才能解锁哪些场景"。约束见 §9.4：**只报告，绝不代填**。

### 10.3 借鉴 IT 可观测平台的四条做法（**不引入任何 IT 品牌名**，见 CLAUDE.md 铁律）

成熟的 IT 运维分析平台（指标侧与日志侧）在"相关性排错、多维下钻"上有四条做法，
对照本仓当前实现，是**真实短板**而非锦上添花：

**① 条件基线（seasonal / contextual baseline）—— 当前缺**

`learn_baseline` 学的是**一条**稳健带（P1/P99+MAD），只在操作员变更点分段。
但 OT 的正常值本身**随上下文变化**：三班制、不同品种/配方、启停机过程。
一条带覆盖所有工况 → 要么过宽（漏真实越限），要么样本混杂被拒学。

借鉴做法：**按上下文分桶学**（班次 / 小时段 / 配方），每桶一条带；
预警时先定位当前上下文再比对。保守原则不变——**桶内样本不够就拒学该桶**，
不用全局带兜底（那会把"没学过这个工况"伪装成"这个工况正常"）。

**② 关系图驱动的相关性 —— 当前缺，且与"拓扑图"是同一件事**

`_proximity_scale` 只按**时间**加权（因在前、果在后）。成熟平台同时按**对象关系**
传播：上游停了，下游缺料是被解释的结果，不该与上游并列成候选根因。

这条把此前当作"可视化需求"的**拓扑/关系图重新定性为分析输入**：
它不是画给人看的图，而是 RCA 的第二根轴。没有它，同一条产线上游停机会持续产生
一串同置信度的下游假根因。

⚠️ 前置：关系从哪来？纯 L3 扫描**拿不到**拓扑（见 §9）。可行来源按可信度排序：
人工声明的产线顺序（最可靠）→ SNMP/LLDP 邻接（网络层，非工艺层）→ 由停机共现
统计推断（最弱，且容易把相关当因果）。**第一条就够启动**，不必等后两条。

**③ 事件聚类（event-type clustering）—— 当前部分**

`alarm_bad_actors` 按 source 聚合。成熟日志平台把**文本相近**的事件先聚成一类再统计，
于是"同一故障的十种措辞"不会被算成十个不同 bad actor。对 OT 告警同样成立
（同一设备不同实例、带流水号的消息）。属增量改进，优先级低于 ①②。

**④ 结论分级与假设账本 —— ~~当前缺~~ 已发布 #195（当天新增,当天实现,2026-08-25 更正）**

> 下面 (a)/(b)/(c) 三个缺口全部已关闭。保留原文是因为它记录了**为什么**这么设计 ——
> 而这条本身就是一个教训:规划写进 HLD 之后当天就实现了,却没回头改,
> 于是文档在几小时内就开始说假话。

来源：研究一份 IT 侧日志分析/RCA 平台的技术材料。**最可迁移的部分恰恰是零模型的
那部分** —— 证据闭环、假设账本、结论分级、时序排除全是确定性的；那份材料自己的
终点是"有状态的 LLM Agent"，而那一条**恰恰不能抄**（与 §1 冲突）。
分辨这两者是本条的全部价值。

当前实现的三个缺口：

**(a) 一个置信度不是一份调查计划。** `_build_hypotheses` 每个假设产出
`cause / confidence / confidence_band / evidence / recommended_action`——
告诉现场"我们认为是什么"，不告诉他"接下来该去取什么"。

借鉴做法：每个假设记四样，而不是一个数字 ——
**支持证据 · 反证 · 缺口 · 下一步**。
**「缺口」是承重的那一项**：它把结论变成计划，明说"要把这条从 Probable 抬到
Confirmed，你得去拿这个"。在 OT 尤其成立，因为那个"这个"往往是**一次现场动作**
（拿振动表去测、开柜看接线、调一次工单），而不是再查一遍已有数据。

**(b) 没有 Excluded —— 而"排除"比"低置信度"强得多。** 今天非首位的原因只是分数低。
但"已排除，理由如下"和"分数低"是两种不同强度的话，现场信任度也不同。

最便宜的一条排除轴**已经在代码里了**：`_proximity_scale` 已经知道某个信号发生在
故障**之后**并打 0.25 折 —— 然后**把理由扔了**。把它变成显式排除几乎零成本：

> 人工操作 · **Excluded** · 该操作发生在首个异常之后，时序不成立

**(c) 没有升级规则 —— 高置信度只是排名在自我认同。** `confidence_band` 由产生排名的
同一批证据算出，所以"高"意味着排名同意它自己，不意味着结论被验证过。

借鉴做法：结论只能靠**排名之外**的东西升到 Confirmed —— 一次直接测量、一次
复现/恢复验证、或一次人工确认。**第三条通道本仓已经建好了**（`iaiops case confirm`，
#180/#191），只是没接到判决上。这与 D24（置信度 + 可靠度两个数字）是同一条脉络：
D24 回答"这个排名背后有多少历史"，本条回答"要更确定还差什么"。

**与 ② 的咬合**：关系图是**第二根排除轴**。上游没停，则下游的假根因不该只是降权，
而应当被 Excluded 并写明理由。②④ 一起做，价值大于各自之和。

> 顺带一面镜子：那份材料给每个产品域都列了"AI 应形成的专业结果"，却**没有一行
> 前置条件** —— 客户得先有拓扑、版本清单、时钟同步、日志留存。这正是
> `docs/ROADMAP.md:414` 点名我们自己销售材料的同一个毛病。OEE 报告已经补上了这一行
> （2026-08-24），这是**我们可以反过来教别人**的地方。

### 10.4 三个关键应用方向（据此排序）

保守基线预警、停机根因（完整）、OEE —— 这三个是主战场。它们的**共同瓶颈不是算法，
是 §9 的就绪度**：基线要样本与上下文，根因要位号清单+告警源+历史库，OEE 要四类
位号映射。**先做 `readiness`/`onboard` 打通前置，再补 10.3 的 ①②**，顺序不能反
——否则做出更聪明的分析，现场依然跑不起来。

---

## 11. 落地顺序:OEE 先行,知识库次之,PdM 最后（2026-08-22 定）

本节是**带市场证据的排序决定**，且**推翻**了 §10.4 里「三个方向并列」的隐含假设。
证据来自 2026-08-22 的公开资料核查，链接见 `docs/MARKET-EVIDENCE.md`。

### 11.1 先承认:这个想法本身零差异化

「案例 → 故障特征 → 权重」的闭环**已经是货架产品**。Aspen Mtell 的公开描述即：
分析 EAM 工单 → 关联历史故障模式 → 生成正常/故障特征签名 → 部署监测复现。
GE Digital APM、Augury、IBM Maximo APM 同层；学术界更进一步（LLM + 领域知识图谱
做 CNC 故障诊断）。

**所以知识库不是护城河，也不是入口 —— 它是留存机制。** 把它当卖点会输给成熟品类；
把它当入场券会死在「客户先免费打工才生效」。

### 11.2 真正的入口:OEE，理由有三条

**① 语义输入最小。** OEE 只要 `run/stop` + `count` + `cycle` —— **三个位号**。
停机 RCA 要完整位号清单 + 告警源 + 历史库。§9 的上手断层在 OEE 这里**最窄**。

**② 有可量化、可当场证明的痛。** 手工 Excel 统计的 OEE **系统性高估 8–12 个百分点**，
因为小停机快到人记不下来。工厂看着自己 75%，实际 63%。这个差距**不需要任何反馈闭环**
就能证明——只要把 tap 接上。

**③ 零标签依赖。** 第一天就有值。知识库让它变好，但不是它生效的前提。

### 11.3 PdM 必须最后

公开数据：**60–70% 的 PdM 项目卡在试点**，75% 的智能工厂项目走不出实验室。
卡住的原因不是算法，是「几百台设备、跨厂、机型不同、维修历史不同、数据系统不同」。
成熟模型要**先积累足够历史**才到 85–95% 准确率。

PdM 是唯一**同时**需要历史与标签的能力。把它放前面 = 把复利当入场券，
正是那 60–70% 的死法。

### 11.4 ⚠️ 阻塞发现:没有任何东西 7×24 在采集

> ⚠️ **本节是 2026-08-22 的审计快照,其阻塞发现已于 #168–#182 解决。**
> 保留是因为它记录了当时的判断依据;**不要把它读成当前状态**。

审计结果（2026-08-22）：

| 现状 | 事实 |
|---|---|
| `opcua monitor --duration-s 10` | **有界窗口**，默认 10 秒 |
| `metrics serve` | 只**读**本地库，不填 |
| 本地库 `~/.iaiops/data.db` | ~~仅在有人手动跑读命令时进数据~~ → **已发布 #168**:`iaiops collect run` |

而 OEE 的**脑子基本齐了**：`downtime_events()` 从状态序列切停机段、`six_big_losses()`、
`oee_multidim()` 多维下钻都在。

> **所以「先做 OEE」的工作量主要不在 OEE 算法，而在「持续采集」这个不存在的基础设施。**

它带来的真实工作：断线缓冲与补采、保留策略、崩溃恢复、开机自启，
并把产品形态从「你运行的 CLI」变成「一直在跑的东西」。

### 11.5 由此产生的部署约束:评估模式必须先于常驻部署

在 OT 网络上装常驻进程**要走变更审批（MOC）**；跑一次的 CLI 不用。
所以采集必须支持**评估模式**：在一台笔记本上跑一周，零常驻部署、零现场改动，
先把 8–12 个点的差距摆到桌上。**拿到证据再谈部署**——顺序反了就卡在审批里。

这不是市场技巧，是本仓既有纪律的延续：`scan plan` 零发包、`readiness` 零接触，
都是同一条「先证明价值，再要权限」。

### 11.6 定位:不做「OEE 软件」

OEE 是**拥挤品类**（Evocon / Fabrico / Symestic / TEEPTRAK / MaintMaster + 每一家 MES）。
以「OEE 软件」入场是又晚又薄的跟随者。

差异在**拿到那个数字的路径**，不在仪表盘：

```
scan（有什么） → readiness（还差什么） → 确认 3 个位号 → 真实 OEE
                                                        ↑
                    零加装硬件、零集成商进场、一个下午
```

友商要么派集成商接线，要么在机台上加按钮盒/传感器。
`scan` + `readiness` 是他们没有的两步，而这两步**已经建好了**。

### 11.7 两条路线不是同一个问题

| | 传感器加装派 | 控制系统 tap（本仓） |
|---|---|---|
| 代表 | Augury / Tractian / 振动传感器厂 | 本仓 |
| 做法 | 贴新传感器（振动/温度，单价已 <$1） | 读 PLC 已经知道的东西 |
| 回答 | 这台电机要坏了吗 | **这条线凌晨 3 点为什么停了 12 分钟** |
| 态势 | 拥挤、正在商品化 | 分散 |

振动传感器**看不见**「上游缺料导致下游停机」——而这正是 OEE 损失待的地方。
两条路线回答的不是同一个问题，不构成正面竞争，也不应互相借位宣传。

## 12. 站点知识库:让它随用随长（设计 → **大部分已发布**,2026-08-25 更正）

> `core/knowledge/`（store/model/cases/case_store）与 `iaiops case` 六个子命令已在 #172/#173/#180/#191 发布。
> 本节**仍然未实现的只有 §12.8（关系）与 §12.9（P&ID 提案）** —— 也就是 ROADMAP §3 剩下的那两条。

### 12.1 现状:能学，但语料只能进口

`rca_weights.learn_cause_weights()` **已经建好**——从已标注事故语料学出每站点的
`{cause: 权重}`。但它的语料**只有两个来源**：

1. `maintenance_log.py` —— 客户的 CMMS 工单导出
2. `cli/diagnostics.py:216` —— 人手写的 JSON 文件

~~**没有任何东西把本仓自己诊断过、人确认过的一次故障存下来。**~~
→ **已发布**(#180/#191):`iaiops case open` 从检测到的停机开一个案子并附上审计轨迹里的动作,
`iaiops case confirm` 记下人的结论。(2026-08-25 更正)
工具用了两年，和第一天一样聪明。**缺的正是「闭」那一下。**

### 12.2 ⚠️ 而进口的那份语料，很可能是噪声

公开常识：CMMS 工单的故障码**出了名的不可靠**——技师随手点下拉框第一个，
于是满库的「Fixed it」「Broken」「Miscellaneous」。工单是**几小时后为了报账**写的，
不是为了诊断。

**所以 `maintenance_log.py` 这座桥必须标注这个前提**，并且不能作为唯一语料来源。

### 12.3 更致命:「人会确认根因」是这个品类第一大失败模式

大量 CMMS 项目失败，正是因为操作员和技师**根本不录**。要是报个问题得登录系统、
翻好几屏、填表，人就不用了——宁可口头告诉班长。**强制推行只会产出低质量数据。**

> 「每次故障都**可以**记录来丰富算法」——这个「可以」是全部风险所在。
> 技术上可以，现场不会。

### 12.4 解法:标签必须是既有工作的副产品，不是新增任务

**关键洞察：`~/.iaiops/audit.db` 已经在记录每一次治理调用**——改了什么、何时、
谁批准；`undo.db` 还存了改前状态。

```
线停了 → 工程师用 iaiops 改设定值救回来
       → 审计库已零成本记下「这次诊断之后，人实际做的动作是 X」
```

**这个标签比工单质量高得多**：发生在诊断当下、在本仓自己的分类体系里、带着证据包。

三条硬约束：

1. **绝大部分标签从审计轨迹自动来**，不靠人录；
2. 需要人时**最多一次点击**——从已排序的 3–5 个假设里点一个，或点「都不是」。
   **永远不出现自由文本框**；
3. **误报归档也是标签**——点掉一条报警就是一个负样本，免费，而且通常比正样本多。

底线：**零标签的第一天产品必须已经有用**（这正是 §11.2 把 OEE 排前的原因）。

### 12.5 知识库的形状

```
站点知识库（本地单文件，owner-only，零外发）
├── 结构   端点 / 位号 / 语义角色 / 上下游关系
├── 基线   分上下文的正常带（§10.3①）
├── 案例   每次事故:工具说了什么 + 人确认了什么 + 什么修好的
├── 权重   从案例推出（learn_cause_weights 已有）
└── 特征   症状组合 → 已知故障
```

`baseline_store` / `alias_store` 已立范式（JSON、0600、原子 temp+replace、绝不写设备），
照抄，不另起炉灶。

### 12.6 规则①（最重要）:三种来源分开标记，永不混淆

| 标记 | 含义 | 可信度 |
|---|---|---|
| `declared` | 人工声明 | 最高 |
| `derived` | 从数据推导 | 中，**必须能出示证据** |
| `suggested` | 启发式猜测 | 最低，**未经确认不得参与推理** |

**`suggested` 一旦被当成 `declared` 用，整个 RCA 变成「自信地错」**——比不出结果危险得多。
这是 D16 在 `readiness` 上已经守住的纪律，知识库必须一模一样。

### 12.7 规则②:两个数字，不是一个

- **置信度** —— 证据多强地指向这个原因（noisy-OR，已有）
- **可靠度** —— **这个数字本身该被信几分**（~~缺~~ **已发布 #193**,2026-08-24 更正）

只记录了 3 个案例的站点给出的 90%，和 300 个案例的 90%，**根本不是一回事**。
今天的输出无法区分，而这正是现场最该看见的差别。这也贴合既有哲学：
`insufficient_data` 宁可拒绝也不假装。

### 12.8 规则③:时间戳只产生候选，绝不自动成边

相关性/因果性陷阱在产线上**必然踩中**：上游一停，下游全部相关。
共现统计只能产出「请人确认这条关系」，**永远不能自己连上图**。

### 12.9 规则④:读设计文档 —— 好想法，但只能落在建议层

P&ID / 工艺流程图**字面上编码了工序顺序**，是最富的关系来源。但抽取会出错，
且这是 LLM 任务，而 brain 层必须零模型（§1）。

**解法**：文档抽取放在**前端/agent**，产出**提案文件**，人确认后才以 `declared`
进知识库。于是知识库永远不依赖模型（离线 App 一字节不差），抽错了也在人这关被拦住。

### 12.10 ⚠️ 自我确认回路

工具说「轴承故障」，操作员没细查就点确认 → 进语料 → 权重朝工具**原本就相信的方向**漂移。
跑几个月，系统会变得非常自信，而且非常错。

**缓解**：只有当人**独立说出**原因（或明确推翻工具结论）才计作标签；
**单独记录人机一致率**——一致率异常高本身就是警报。

### 12.11 场景清单（按价值排）

| # | 场景 | 为什么值钱 |
|---|---|---|
| 1 | **变更影响预判** | 写之前告诉你「这个位号喂着下游 3 个工位」。治理层已有 dry-run + 双确认，**但预览目前是空洞的**——只说改哪个寄存器，不说波及谁。关系图接上，预览才有意义。直接打**除险**定位，不是 AI |
| 2 | **交接班 / 知识传承** | 老师傅退休带走的东西，正是案例库要留下的。**离线 App 天然强于云的场景** |
| 3 | **相似历史检索** | 「这次像 3 月那次 #47」——特征最近邻，纯本地、零模型、算得飞快，演示极具说服力 |
| 4 | **误报归档** | 负样本免费获得，基线与权重同时受益 |
| 5 | **维修方案沉淀** | 「这个特征出现过 9 次，7 次是换密封」 |
| 6 | **就绪度曲线** | `readiness` 按时间记录 = 「这个站点在变聪明」的证据，续费谈判唯一硬的东西 |

## 13. 调查是一个对象，不是一次命令（2026-08-26 定）

### 13.1 来源，以及这一次可迁移的边界

与 §10.3④ 同一份 IT 侧材料。上一轮取走的是**打分那一半** —— 证据闭环里的
「验证假设」与「结论分级」，落成 D28/D29/D30。这一轮取走的是**容器那一半**：
八个步骤本身，以及「每一步可暂停、可下钻、可审计、并能回到原始证据」。

**同样两条不能抄：**

1. 那份材料的终点是「有状态的 LLM Agent」。本仓的八步状态机**必须是确定性的**
   （§1，AST 守卫测试）。理由不是洁癖——现场常常没网、或客户不允许接模型，
   而那正是最需要走完八步的时刻。LLM 只能是**读写这个对象的前端**。
2. 词汇一个都不能进（CLAUDE.md 铁律，CI 已守）。搬的是**方法**，不是**词**。

### 13.2 为什么必须是对象

今天的三样东西各自都在，但**没有任何东西把它们串成一次调查**：

| 有的 | 缺的 |
|---|---|
| `diag rca` / `rca-live` —— 一次性裁决 | 走完就没了，不能暂停、不能继续 |
| `case` —— `{when, label, ranked, capture}` | **不存证据、不存时间线、不存步骤状态** |
| `audit.db` —— 哈希链 | 记的是「谁调用了什么」，不是「这次调查走到哪」 |

「可暂停 · 可下钻 · 可审计 · 能回到原始证据」这四个能力，**每一个都要求状态**。
没有容器，八步就只是文档里的八个词。

**还有一条今天现挣出来的理由。** 2026-08-26 一次演示走查抓到 7 个缺陷（#198–#202），
它们的共同形状是**客户真正会走的那条路从来没有人走过**：historian 没有可达的写入
路径、`push` 够不着 `--transport`、RCA 把自己连不上当成传感器故障。
**一个逐步走客户的路、并在每一步报告「还缺什么」的产品，是这一类缺陷的结构性解药** ——
它把「路走不通」从一个要靠人偶然发现的事，变成一个产品自己会说出来的事。

### 13.3 对象形状

```
Investigation                        ← 新增
  ├─ mode: dry | live
  ├─ scope: 对象 · 时间窗 · 影响范围   ← 第 01 步的产物
  ├─ steps[8]: StepStatus
  │    ├─ requirements: [Requirement]  ← 原样复用 core/readiness 的结构
  │    ├─ evidence_ids: [...]          ← 指回原始证据，可下钻
  │    └─ state: blocked | partial | done | skipped(附理由)
  ├─ timeline                         ← 第 05 步的产物
  ├─ ledger                           ← 假设账本（D30，已有）
  └─ conclusion                       ← 分级 RCA（D28，已有）
```

`case` **保持原样**：它是这个容器**产出**的东西（已确认标签 → 学习闭环），
不是被它取代。一次 live 调查以一个 case 开始，以一次 `case confirm` 结束。

### 13.4 八步 → OT 的映射与缺口地图

**这张表是本节的承重部分。** 「本仓现状」一列必须随实现更新，写错的代价是
让人以为某一步已经能走。

| # | 步骤 | OT 对应 | 本仓现状 |
|---|---|---|---|
| 01 | 定义事件 | 端点 · 位号 · 时间窗 · 影响的产线段 | **部分**：`case open` 只能从停机产生，不能自定义范围 |
| 02 | 收集证据 | 采样历史 · 告警 · 历史库 · 变更基线 · 关系 | **部分**：前四个有（`collect`/`historian`/`baseline`）；关系无（§10.3②） |
| 03 | 规范与校验 | 统一时间与实体、检查丢失与时钟偏差 | **部分**：盲区检测与 UTC 归一已有（#199），但不是显式的一步 |
| 04 | 压缩与排序 | 告警去重 · 洪泛分段 · 合理化 | **有**：`diag alarm-flood` / `alarm-worksheet`（ISA-18.2） |
| 05 | 关联时间线 | Trigger · Symptom · Propagation · Recovery | **有**（2026-08-27）：`core/brain/timeline`，四段需声明的 run_state；传播只走已声明关系 |
| 06 | 验证假设 | 支持证据 · 反证 · 缺口 · 下一步 | **有**：D30 |
| 07 | 知识校验 | 设备型号 · 固件 · 适用条件 · 历史案例 | **几乎无** —— 见 §13.8 |
| 08 | 结论与闭环 | 分级 RCA · 处置 · 恢复验证 · 回灌知识 | **有**：D28/D29 + `case confirm` → `learn-weights` |

两个真正的洞是 **05** 和 **07**。其余是「已有但没串起来」。

### 13.5 一个模型，两种 scope 来源 —— **不按人分**

初稿把 dry / live 分给「集成商」和「现场的人」。**那个划分是假的**：在 OT 现场，
去做评估的人和停机当下在场的人常常就是同一个人（或同一家集成商的同一支队伍）。
按人分会造出两套界面、两套逻辑，去服务一个并不存在的差异。

真正的差别只有一条 —— **scope 从哪来**：

| 模式 | scope 来源 | 问的问题 |
|---|---|---|
| dry | 假设的（「如果一号线明早停机」） | 真出事时这个站**能答到第几步** |
| live | 一个真实 case | 这次事故，**每一步我们实际有什么** |

八步的判定逻辑**一行都不分叉**。dry 因此是 `readiness` 的自然延伸：
`readiness` 回答「这个站现在能跑哪些**场景**」，dry 调查回答「这个站现在能走完
**几步调查**」—— 同一个 `Requirement` 结构，两个维度。

### 13.5.1 真正需要分割的是环境与厂区

需要隔离的从来不是**人**，是**边界**：生产线 vs 测试台，A 厂区 vs B 厂区。
本仓已有的 `--site` 就是这个边界（`case_store` 按站点、`learn-weights --site`、
知识库按站点），调查对象必须落在同一个边界里。

**后果是硬的：跨边界的知识不自动适用。** 在测试台上确认的一个根因，
不得抬高生产线同类假设的置信度 —— 那两条产线的设备型号、批次、工况都可能不同，
而 D24 的「可靠度」正是用来表达这件事的：可靠度是**本边界内**的案例数，
不是全公司的。混算会把「我们在测试台见过一次」讲成「这条线我们很有把握」。

同一个站点下的**多条产线**是 scope，不是边界：它们共享知识与权重，
由 `scope.asset` 区分。**厂区/环境才是边界。**

### 13.6 `expressible` 升为调查层的一等公民

`core/readiness/model.py::Requirement.expressible` 的原注释是：

> False when the product offers no way to supply this yet —— 因为「你没有配」和
> 「你配不了」把人送去两个完全不同的地方。

调查层把这个字段**摆到台面上**。现场最需要的一句话不是「缺数据」，
而是**「这一项目前这个产品根本没法让你提供」** —— 那决定他是现在打个电话补配置，
还是把这条写进给客户的差距报告。

这也是 2026-08-26 那批缺陷的共同标签：能力在代码里、客户够不着。
**调查层必须把这类情况说出来，而不是显示成一个空值。**

### 13.7 第 05 步：时间线是证据的**重排**，不是新推理

Trigger / Symptom / Propagation / Recovery 四段，看起来最像「智能」，
恰恰最容易变成编故事。约束两条：

1. **每一段的每一条都必须指回一个已存在的证据 ID。** 时间线不产生新事实。
2. **Propagation 的边只能来自已声明的关系**（D25：时序共现只产生待确认候选，
   绝不自动成边）。产线上游一停下游全相关，靠时序推因果必然踩中。

因此 05 的前置是 §10.3② 的关系轴，而关系的第一来源是**人工声明的产线顺序** ——
`iaiops relations declare <上游> <下游> --by <你>`（2026-08-27 落地，
存为 `declared` 事实、按站点隔离、拒绝自环与成环）。
在关系为空时，05 仍然可用：它退化为一条**单资产**时间线，并在
`requirements` 里明说「跨资产传播需要产线上下游关系，当前未声明」。
**退化必须显式，不能静默出一条看起来完整的时间线。**

### 13.8 第 07 步：没有知识库，就必须说没有

现状核对（2026-08-26）：故障机制与因果权重是**硬编码的 Python 常量**
（`CAUSE_KEYWORDS` 七类 + 十一个 `W_*`），`KNOWN_CAUSES` 是 frozenset。
「知识」这一层实际存在的只有 `case_store`（本站已确认事故）、`diag corpus`
（CMMS 工单导入）与 `learn-weights`（按站点调权重）。

**没有 KB、没有每协议知识库、没有设备机制库、没有厂商文档/适用条件绑定。**

所以第 07 步的正确行为是 `expressible=False` + 一句「本产品尚无知识库挂载途径」，
**而不是跳过这一步**（跳过读起来像「校验过了，没问题」）。

这一步也划定了 §12 站点知识库的落点：它长出来的时候，接的就是 07 这个插槽。
§12.6 的三源分离（`declared`/`derived`/`suggested`，D23）在这里继续成立。

### 13.9 落点与前端

**引擎**：新增 `iaiops/core/investigate/`，与 `core/readiness/` 平级。
纯函数 + 确定性状态机，落在四层架构的 **brain/runtime 那一层**，
因此受 §1 的零模型守卫覆盖。

**持久化**：每次调查一个 JSON，跟随 `core/collect/session.py` 的形状
（原子写、0600、带 `version`）。

> ⚠️ **本条在实现时被修正（2026-08-26）。** 初稿写的是「走
> `core/knowledge/store.py` 既有的站点知识库，而不是新开一个库」——
> 在代码上站不住:`KnowledgeBase` 是**只追加的事实集合**，而一次调查是
> **会变的活动记录**。`sessions/` 才是形状对得上的先例。
> 知识库仍然是**结论**的落点（经 `case confirm`），这一条没变。

每一步的推进都是一次受治理调用（`@governed_tool`），审计链因此天然覆盖
「这次调查走到哪、谁推进的」。

**前端**：沿用 §10.1 的定论，不新发明 ——

| 前端 | 为什么 | 次序 |
|---|---|---|
| CLI | 边缘盒 / 无 GUI 工控机 / CI 里都能跑，无前置 | 先 |
| 自包含 HTML 报告 | 可转发、断网可开（复用 `core/report/`） | 同期 |
| App 页面 | 需要人逐行看/勾的步骤（点表确认、时间线核对），表格远胜命令行 | 后 |
| MCP | agent 代跑 | 最后，且**不得**成为唯一入口 |

次序理由**不是**「谁先用」——§10.1 已定：同一个引擎产出结构化结果，
CLI 渲染成文本、页面渲染成表格。CLI 先，是因为它前置最少、跑得到的地方最多。

最后一条是硬的：**agent 前端可以有，但八步必须在没有 agent 时同样走得完。**

### 13.10 交付顺序（依赖决定，不是偏好）

1. **对象 + 八步缺口地图（dry 模式）** —— `Investigation` 与 `StepStatus` 落地，
   八步各自只做 `requirements` 判定，不做分析。产出：一份「这个站能走到第几步」
   的报告。**这一步交付完就已经有东西可交付了**，而且它本身是后两步的地基。
2. **live 模式接上已有能力** —— 02/03/04/06/08 直接接 `collect`/`alarm-flood`/
   `rca`/`case`，把已经存在的东西串起来。不新增分析。
3. ~~**补 05 时间线**~~ —— **已完成 2026-08-27**：`iaiops relations` + `core/brain/timeline`。
   两条实测得来的约束值得记：**计数器不是事件**（真实数据上运行状态变化率 0.02、
   产量计数器 0.77，时间线被计数器淹没到 500 条上限，真正的 trigger 被埋掉），
   以及**截断必须出声**。
4. **补 07 知识库插槽** —— 与 §12 合流。
5. **App 页面与 MCP 前端** —— 引擎稳定之后。

边缘打包（Margo / 西门子 Industrial Edge 应用包、签名镜像）**已经存在**，
新增命令自动随之交付，不构成独立工作项。

## 14. 决策记录（续 §6、§13）

| # | 决策 | 理由 |
|---|---|---|
| D19 | **OEE 先行**，知识库次之，PdM 最后 | OEE 语义输入最小（3 个位号）、痛可量化（手工高估 8–12 点）、零标签依赖；PdM 同时需要历史与标签，60–70% 的项目死在把复利当入场券 —— §11 |
| D20 | 知识库是**留存机制**，不是护城河、不是入口 | 闭环已是货架产品（Aspen Mtell 等）。当卖点会输给成熟品类，当入场券会死在「客户先免费打工」 —— §11.1 |
| D21 | 采集必须支持**评估模式**（笔记本跑一周），先于常驻部署 | OT 网络上的常驻进程要走 MOC 审批；先拿证据再要权限，与 `scan plan` 零发包同源 —— §11.5 |
| D22 | 标签**主要从审计轨迹自动获得**；人工确认最多一次点击，永不自由文本 | 「操作员会录入」是本品类第一大失败模式；审计库已零成本记录了人在事故后实际做的动作 —— §12.3/§12.4 |
| D23 | 知识库每条事实标记 `declared` / `derived` / `suggested`，三者永不混淆 | `suggested` 当 `declared` 用 = 自信地错，比不出结果危险 —— §12.6，D16 的延伸 |
| D24 | 输出**两个数字**：置信度 + 可靠度 | 3 个案例的 90% 与 300 个案例的 90% 不是一回事 —— §12.7 |
| D25 | 时间戳共现只产生**待确认候选**，绝不自动成为关系边 | 上游一停下游全相关，产线上必然踩中相关≠因果 —— §12.8 |
| D26 | 产线设计文档抽取属**前端/agent**，产出提案由人确认后才入库 | 保住 brain 层零模型（§1），离线 App 能力不打折 —— §12.9 |
| D27 | 不以「OEE 软件」入场 | 拥挤品类。差异在拿到数字的**路径**（scan + readiness + 零加装 + 一个下午），不在仪表盘 —— §11.6 |
| D28 | RCA 结论四级：**Candidate / Probable / Confirmed / Excluded** | 「已排除，理由如下」和「分数低」是两种强度的话；只有前者能让现场停止在那条分支上花时间 —— §10.3④(b) |
| D29 | 升到 **Confirmed 必须来自排名之外**：直接测量、复现/恢复验证、或人工确认 | 由同一批证据算出的高置信度，只是排名在自我认同。人工确认通道已建好（`case confirm`），接上即可 —— §10.3④(c)，D24 的延伸 |
| D30 | 每个假设记 **支持证据 · 反证 · 缺口 · 下一步**，不只记一个分数 | 「缺口」把结论变成调查计划。OT 的缺口常是**一次现场动作**，不是再查一遍已有数据 —— §10.3④(a) |
| D31 | 一次调查是**有状态的对象**，不是一次命令 | 「可暂停 · 可下钻 · 可审计 · 回到原始证据」四个能力每一个都要求状态；今天 `case` 只存 `{when,label,ranked}`，`diag rca` 走完即弃 —— §13.2 |
| D32 | 八步状态机**必须确定性**；LLM 只能是读写它的前端 | 现场没网、或客户不许接模型时，正是最需要走完八步的时刻。§1 的零模型守卫直接覆盖它 —— §13.1 |
| D33 | dry / live 只差 **scope 从哪来**，判定逻辑不分叉；**不按人分** | 去做评估的人和事故当下在场的人常是同一个人，按人分会造出两套界面去服务一个并不存在的差异 —— §13.5 |
| D34 | 隔离边界是**厂区 / 环境**，不是人；跨边界的知识不自动适用 | 测试台上确认的根因不得抬高生产线的置信度。D24 的可靠度是**本边界内**的案例数，混算会把「见过一次」讲成「很有把握」 —— §13.5.1 |
| D35 | 时间线只做**证据重排**：每条指回已有证据 ID，Propagation 边只来自已声明关系 | 四段式最像「智能」，也最容易变成编故事。D25 的直接应用；关系为空时显式退化为单资产时间线 —— §13.7 |
| D36 | 缺知识库时第 07 步标 `expressible=False`，**不跳过** | 跳过读起来像「校验过了，没问题」。今天故障机制是硬编码常量，KB 为零 —— §13.8 |
